language: java
jdk:
  - openjdk8

node_js: "0.10"
install: "npm install"

branches:
  only:
    - master

# Travis CI 서버의 Home
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'

script:
    - "npm test"
    - "./gradlew clean build"

# CI 실행 완료시 메일로 알람
notifications:
email:
  recipients:
    - jssongjk637@gmail.com